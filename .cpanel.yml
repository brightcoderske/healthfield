---
deployment:
  tasks:
    # Install the production build compiled and committed from the local machine.
    - /home/healthfi/nodevenv/healthfield/24/bin/node /home/healthfi/healthfield/scripts/deploy-cpanel.mjs
