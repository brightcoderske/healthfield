"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";
type Message={id:number;message:string;createdAt:string;firstName:string;role:string};
export function ChatPanel({conversationId}:{conversationId?:number}){const [messages,setMessages]=useState<Message[]>([]),[text,setText]=useState(""),[sending,setSending]=useState(false);const load=useCallback(async()=>{const response=await fetch(`/api/chats${conversationId?`?conversation=${conversationId}`:""}`,{cache:"no-store"});if(response.ok)setMessages((await response.json()).messages)},[conversationId]);useEffect(()=>{load();const timer=setInterval(load,10000);return()=>clearInterval(timer)},[load]);async function submit(e:FormEvent){e.preventDefault();if(!text.trim())return;setSending(true);const response=await fetch("/api/chats",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:text,conversationId})});if(response.ok){setText("");await load()}setSending(false)}return <main className="chat-page"><header><a href={conversationId?"/admin/chats":"/account"}>← Back</a><div><h1>{conversationId?"Customer conversation":"Chat with Healthfield"}</h1><p>Ask about products, prescriptions, delivery or an order.</p></div></header><section><div className="chat-messages">{messages.length?messages.map(message=><article className={message.role==="CUSTOMER"?"customer":"team"} key={message.id}><strong>{message.role==="CUSTOMER"?message.firstName:"Healthfield team"}</strong><p>{message.message}</p><small>{new Date(message.createdAt).toLocaleString()}</small></article>):<div className="chat-empty"><strong>How can we help?</strong><p>Send a message and our pharmacy team will reply here.</p></div>}</div><form onSubmit={submit}><textarea aria-label="Message" value={text} onChange={e=>setText(e.target.value)} placeholder="Type your message…" rows={2}/><button disabled={sending}>{sending?"Sending…":"Send"}</button></form></section></main>}
