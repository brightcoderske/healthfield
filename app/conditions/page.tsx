import Image from "next/image";
import { backendJson } from "@/lib/backend-api";
export const dynamic="force-dynamic";
export default async function ConditionsPage(){const {conditions}=await backendJson<{conditions:Array<{id:number;name:string;slug:string;description:string|null}>}>("/v1/views/conditions");return <main className="conditions-page"><header><a href="/"><Image src="/healthfield-logo-clean.png" alt="Healthfield Pharmacy" width={210} height={76} priority/></a><a href="/">← Continue shopping</a></header><section><span>Healthfield product guide</span><h1>Shop by health condition</h1><p>Select a condition to view matching products in the Healthfield catalogue.</p><div>{conditions.map(condition=><a key={condition.id} href={`/?condition=${condition.slug}#products`}><strong>{condition.name}</strong>{condition.description&&<small>{condition.description}</small>}<b>View products →</b></a>)}</div></section></main>}
