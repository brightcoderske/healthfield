import type{Metadata}from"next";import Image from"next/image";import Link from"next/link";import{notFound}from"next/navigation";import{BackendError,backendJson}from"@/lib/backend-api";import{RichText}from"@/app/products/rich-text";
type Post={title:string;excerpt:string;content:string;imageUrl:string|null;metaTitle:string|null;metaDescription:string|null;publishedAt:string|null};async function get(slug:string){try{return(await backendJson<{post:Post}>(`/v1/views/blogs/${encodeURIComponent(slug)}`)).post}catch(e){if(e instanceof BackendError&&e.status===404)return null;throw e}}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const post=await get((await params).slug);return post?{title:post.metaTitle||post.title,description:post.metaDescription||post.excerpt}:{} }
export default async function Article({params}:{params:Promise<{slug:string}>}){const post=await get((await params).slug);if(!post)notFound();return <main className="blog-article"><header><Link href="/"><Image src="/healthfield-logo-clean.png" alt="Healthfield Pharmacy" width={210} height={75}/></Link><Link href="/blog">← Health guide</Link></header><article><span>Pharmacist health guide</span><h1>{post.title}</h1><p className="lead">{post.excerpt}</p>{post.imageUrl&&<img src={post.imageUrl} alt={post.title}/>}<RichText value={post.content}/><aside>Health information is general and does not replace diagnosis or advice from your doctor or pharmacist.</aside></article></main>}
