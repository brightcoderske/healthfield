import type{Metadata}from"next";import{backendPublicJson}from"@/lib/backend-api";import{BlogCatalogue}from"./blog-catalogue";export const metadata:Metadata={title:"Health and Medicine Advice",description:"Practical pharmacist-reviewed health, medicine and wellness information from Healthfield Pharmacy in Juja and Nairobi."};type Post={id:number;title:string;slug:string;excerpt:string;imageUrl:string|null;publishedAt:string|null};export default async function Blog(){const{posts}=await backendPublicJson<{posts:Post[]}>("/v1/views/blogs",300);return <BlogCatalogue posts={posts}/>}
