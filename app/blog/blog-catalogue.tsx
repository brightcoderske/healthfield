"use client";import{Search,ShoppingCart}from"lucide-react";import Image from"next/image";import Link from"next/link";import{useMemo,useState}from"react";type Post={id:number;title:string;slug:string;excerpt:string;imageUrl:string|null;publishedAt:string|null};export function BlogCatalogue({posts}:{posts:Post[]}){const[query,setQuery]=useState(""),filtered=useMemo(()=>posts.filter(post=>`${post.title} ${post.excerpt}`.toLowerCase().includes(query.toLowerCase())),[posts,query]);return <main className="blog-page"><header className="blog-store-header"><Link href="/"><Image src="/healthfield-logo-clean.png" alt="Healthfield Pharmacy" width={220} height={78}/></Link><label><Search/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search health and medicine articles" aria-label="Search blog articles"/></label><Link className="blog-cart" href="/cart" aria-label="View cart"><ShoppingCart/></Link></header><div className="blog-heading"><span>Healthfield health guide</span><h1>Pharmacy and wellness articles</h1><p>Clear, responsible health information from your pharmacy team.</p></div><section>{filtered.map(post=><article key={post.id}>{post.imageUrl&&<img src={post.imageUrl} alt=""/>}<div><small>{post.publishedAt?new Date(post.publishedAt).toLocaleDateString("en-KE"):""}</small><h2><Link href={`/blog/${post.slug}`}>{post.title}</Link></h2><p>{post.excerpt}</p><Link href={`/blog/${post.slug}`}>Read article →</Link></div></article>)}{!filtered.length&&<div className="blog-empty"><Search/><strong>No matching articles</strong><p>Try a different health topic or medicine name.</p></div>}</section></main>}
