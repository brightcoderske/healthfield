import { backendJson } from "@/lib/backend-api";
function xml(value:string|number){return String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&apos;")}
type Product={id:number;sku:string;name:string;description:string|null;shortDescription:string|null;imageUrl:string|null;price:string;discountPrice:string|null;brand:string|null;barcode:string|null;packSize:string|null;category:string};
export async function GET(){const origin=(process.env.APP_URL||"https://healthfieldpharmacy.co.ke").replace(/\/$/,""),rows=(await backendJson<{products:Product[]}>("/v1/views/merchant")).products,items=rows.filter(row=>row.imageUrl).map(row=>{const description=row.description||row.shortDescription||`${row.name}${row.packSize?` - ${row.packSize}`:""}.`;return `<item><g:id>${xml(row.sku||`hf-${row.id}`)}</g:id><title>${xml(row.name)}</title><description>${xml(description)}</description><link>${origin}/products/${row.id}</link><g:image_link>${xml(row.imageUrl!)}</g:image_link><g:availability>in_stock</g:availability><g:price>${Number(row.discountPrice??row.price).toFixed(2)} KES</g:price><g:condition>new</g:condition>${row.brand?`<g:brand>${xml(row.brand)}</g:brand>`:""}${row.barcode?`<g:gtin>${xml(row.barcode)}</g:gtin>`:`<g:identifier_exists>no</g:identifier_exists>`}<g:product_type>${xml(row.category)}</g:product_type></item>`}).join(""),feed=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0" xmlns:g="http://base.google.com/ns/1.0"><channel><title>Healthfield Pharmacy</title><link>${origin}</link><description>Medicines, skincare, wellness and personal-care products from Healthfield Pharmacy.</description>${items}</channel></rss>`;return new Response(feed,{headers:{"Content-Type":"application/xml; charset=utf-8","Cache-Control":"public, max-age=3600, stale-while-revalidate=86400"}})}
