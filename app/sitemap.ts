import type { MetadataRoute } from "next";import { backendJson } from "@/lib/backend-api";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const origin=(process.env.APP_URL||"https://healthfieldpharmacy.co.ke").replace(/\/$/,""),staticPaths=["","/about","/contact","/faq","/conditions","/prescriptions/upload","/shipping-policy","/returns-policy","/privacy-policy","/terms"];let productRows:Array<{id:number;updatedAt:string}>=[];try{productRows=(await backendJson<{products:Array<{id:number;updatedAt:string}>}>("/v1/views/sitemap")).products}catch{}return[...staticPaths.map(path=>({url:`${origin}${path}`,lastModified:new Date(),changeFrequency:path===""?"daily" as const:"monthly" as const,priority:path===""?1:.6})),...productRows.map(product=>({url:`${origin}/products/${product.id}`,lastModified:new Date(product.updatedAt),changeFrequency:"weekly" as const,priority:.8}))]}
