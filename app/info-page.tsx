import Image from "next/image";
export type InfoSection={title:string;paragraphs?:string[];items?:string[]};
export function InfoPage({kicker,title,intro,sections}:{kicker:string;title:string;intro:string;sections:InfoSection[]}){return <main className="info-page"><header><a href="/"><Image src="/healthfield-logo-clean.png" alt="Healthfield Pharmacy" width={210} height={75}/></a><a href="/#products">Continue shopping</a></header><article><span>{kicker}</span><h1>{title}</h1><p className="info-intro">{intro}</p>{sections.map(section=><section key={section.title}><h2>{section.title}</h2>{section.paragraphs?.map(text=><p key={text}>{text}</p>)}{section.items&&<ul>{section.items.map(item=><li key={item}>{item}</li>)}</ul>}</section>)}<p className="policy-note">Last updated: 31 July 2026. Contact Healthfield Pharmacy if you need clarification about this information.</p></article></main>}
