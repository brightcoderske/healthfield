"use client";
import { FormEvent, useState } from "react";
export default function CatalogueSetupPage(){const [message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>,kind:"categories"|"conditions"){event.preventDefault();const form=event.currentTarget,data=new FormData(form),response=await fetch(`/api/${kind}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:data.get("name"),description:data.get("description")})}),result=await response.json().catch(()=>({}));setMessage(response.ok?`${result.name} added successfully.`:result.error||"Could not add item.");if(response.ok)form.reset()}return <main className="compact-admin-page taxonomy-page"><header><div><a href="/admin">← Dashboard</a><h1>Categories & conditions</h1><p>Add options that immediately become available in the product editor.</p></div></header>{message?<div className="form-message">{message}</div>:null}<section><form onSubmit={event=>submit(event,"categories")}><h2>Add category</h2><label>Name<input name="name" maxLength={150} required/></label><button>Add category</button></form><form onSubmit={event=>submit(event,"conditions")}><h2>Add health condition</h2><label>Name<input name="name" maxLength={150} required/></label><label>Description<textarea name="description" maxLength={500} rows={3}/></label><button>Add condition</button></form></section></main>}
