"use client";
import { FormEvent,useState } from "react";
type Item={id:number;name:string;description?:string|null};
function List({title,kind,initial}:{title:string;kind:"categories"|"conditions";initial:Item[]}){const [items,setItems]=useState(initial),[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget),payload={name:String(form.get("name")),description:String(form.get("description")||"")},res=await fetch(`/api/${kind}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)}),data=await res.json().catch(()=>({}));if(!res.ok)return setMessage(data.error||"Could not save.");setItems(rows=>[...rows,{id:data.id,...payload}]);e.currentTarget.reset()}async function edit(item:Item){const name=window.prompt(`Rename ${title.slice(0,-1)}`,item.name);if(!name)return;const res=await fetch(`/api/${kind}/${item.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,description:item.description||""})});if(res.ok)setItems(rows=>rows.map(row=>row.id===item.id?{...row,name}:row))}async function remove(item:Item){if(!window.confirm(`Remove ${item.name}? Products already using it keep their historical relation.`))return;const res=await fetch(`/api/${kind}/${item.id}`,{method:"DELETE"});if(res.ok)setItems(rows=>rows.filter(row=>row.id!==item.id))}return <section className="taxonomy-card"><header><h2>{title}</h2><span>{items.length} records</span></header><form onSubmit={submit}><input name="name" placeholder={`New ${title.slice(0,-1).toLowerCase()}`} required/><input name="description" placeholder={kind==="conditions"?"Optional description":"Not needed for categories"} disabled={kind==="categories"}/><button>Add</button></form>{message&&<p>{message}</p>}<div className="taxonomy-table"><div className="taxonomy-table-head"><span>Name</span><span>Actions</span></div>{items.map(item=><article key={item.id}><span><b>{item.name}</b>{item.description&&<small>{item.description}</small>}</span><span><button onClick={()=>edit(item)}>Edit</button><button onClick={()=>remove(item)}>Delete</button></span></article>)}</div></section>}
export function TaxonomyManager({initialCategories,initialConditions}:{initialCategories:Item[];initialConditions:Item[]}){return <main className="data-page taxonomy-page"><header><a href="/admin">← Dashboard</a><h1>Categories/Conditions</h1></header><div className="taxonomy-grid"><List title="Categories" kind="categories" initial={initialCategories}/><List title="Conditions" kind="conditions" initial={initialConditions}/></div></main>}
