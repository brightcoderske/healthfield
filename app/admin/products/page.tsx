import{backendJson}from"@/lib/backend-api";import{requireRole}from"@/lib/auth";import{ProductManager}from"./product-manager";export const dynamic="force-dynamic";type Product={id:number;categoryId:number;name:string;sku:string;barcode:string|null;brand:string|null;packSize:string|null;shortDescription:string|null;description:string|null;price:number;discountPrice:number|null;costPrice:string|number|null;costPriceEstimated:boolean;imageUrl:string|null;prescriptionRequired:boolean;isFeatured:boolean;isActive:boolean;conditionIds:number[]};type Option={id:number;name:string};export default async function AdminProductsPage(){
// Read from the session rather than the API payload: the role is known here, so the
// screen never has to wait on a redeployed API to decide what an owner may see. The
// API enforces the same rule on the data — this only decides what is drawn.
const session=await requireRole(["ADMIN","SUPER_ADMIN"]);const data=await backendJson<{products:Product[];categories:Option[];conditions:Option[];canEditCost?:boolean;branches?:Array<{id:number;name:string}>;stock?:Array<{branchId:number;productId:number;quantityAvailable:number;reorderLevel:number}>}>("/v1/views/admin/products");return <ProductManager initialProducts={data.products} categories={data.categories} conditions={data.conditions} branches={data.branches} stock={data.stock} canEditCost={session.role==="SUPER_ADMIN"}/>}
