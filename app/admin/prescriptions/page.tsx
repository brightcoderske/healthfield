import { backendJson } from "@/lib/backend-api";import { PrescriptionManager } from "./prescription-manager";export const dynamic="force-dynamic";type Item={id:number;senderName:string|null;originalFilename:string;createdAt:string;status:string;mimeType:string;pharmacistNotes:string|null};export default async function PrescriptionsPage(){const {prescriptions}=await backendJson<{prescriptions:Item[]}>("/v1/views/admin/prescriptions");return <main className="data-page"><header><a href="/admin">← Dashboard</a><h1>Prescriptions</h1></header><PrescriptionManager initialItems={prescriptions}/></main>}
