import { backendJson } from "@/lib/backend-api";
export default async function ProductLayout({children,params}:{children:React.ReactNode;params:Promise<{id:string}>}){const id=Number((await params).id);let data:{product:{id:number;name:string;description:string|null;shortDescription:string|null;imageUrl:string|null;sku:string;barcode:string|null;brand:string|null;price:string;discountPrice:string|null}}|null=null;try{data=await backendJson(`/v1/views/products/${id}`)}catch{}if(!data)return children;const product=data.product,origin=(process.env.APP_URL||"https://healthfieldpharmacy.co.ke").replace(/\/$/,""),price=Number(product.discountPrice??product.price),jsonLd={"@context":"https://schema.org","@type":"Product",name:product.name,description:product.description||product.shortDescription||`Buy ${product.name} from Healthfield Pharmacy.`,image:product.imageUrl?[product.imageUrl]:undefined,sku:product.sku,gtin:product.barcode||undefined,brand:{"@type":"Brand",name:product.brand||"Healthfield Pharmacy"},offers:{"@type":"Offer",url:`${origin}/products/${product.id}`,priceCurrency:"KES",price:price.toFixed(2),availability:"https://schema.org/InStock",itemCondition:"https://schema.org/NewCondition"}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd).replace(/</g,"\\u003c")}}/>{children}</>}
