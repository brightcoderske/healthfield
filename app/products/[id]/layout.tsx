import { eq } from "drizzle-orm";
import { getDb } from "@/db";
import { products } from "@/db/schema";
export default async function ProductLayout({children,params}:{children:React.ReactNode;params:Promise<{id:string}>}){const id=Number((await params).id),[product]=await getDb().select().from(products).where(eq(products.id,id)).limit(1);if(!product)return children;const origin=(process.env.APP_URL||"https://healthfieldpharmacy.co.ke").replace(/\/$/,""),price=Number(product.discountPrice??product.price);const image=product.imageUrl?(product.imageUrl.startsWith("http")?product.imageUrl:`${origin}${product.imageUrl}`):undefined;const jsonLd={"@context":"https://schema.org","@type":"Product",name:product.name,description:product.description||product.shortDescription||`Buy ${product.name} from Healthfield Pharmacy.`,image:image?[image]:undefined,sku:product.sku,gtin:product.barcode||undefined,brand:{"@type":"Brand",name:product.brand||"Healthfield Pharmacy"},offers:{"@type":"Offer",url:`${origin}/products/${product.id}`,priceCurrency:"KES",price:price.toFixed(2),availability:"https://schema.org/InStock",itemCondition:"https://schema.org/NewCondition"}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd).replace(/</g,"\\u003c")}}/>{children}</>}
