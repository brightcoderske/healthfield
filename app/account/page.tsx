import { Package, ShoppingCart } from "lucide-react";import Link from "next/link";import { backendJson } from "@/lib/backend-api";import { requireRole } from "@/lib/auth";export const dynamic="force-dynamic";type Data={orders:Array<{id:number;orderNumber:string;createdAt:string;fulfilmentMethod:string;status:string;total:string}>;catalog:Array<{id:number;name:string;imageUrl:string|null;packSize:string|null;price:string;discountPrice:string|null}>;prescriptions:Array<{id:number;originalFilename:string;status:string;pharmacistNotes:string|null;createdAt:string}>};export default async function AccountPage(){const user=await requireRole(["CUSTOMER"]),data=await backendJson<Partial<Data>>("/v1/views/account"),orders=data.orders||[],rx=data.prescriptions||[],catalog=data.catalog||[];return <main className="customer-account compact-account"><header><Link href="/#products">← Continue shopping</Link><form action="/api/auth/logout" method="post"><button>Sign out</button></form></header><div className="account-welcome"><span>My Healthfield</span><h1>Hello, {user.firstName}</h1><p>Shop, track orders, prescriptions and pharmacy support.</p></div><section className="account-orders account-table" id="orders"><div><h2>My orders</h2><Link href="/#products">Shop more</Link></div><header><span>Order</span><span>Status</span><span>Amount</span></header>{orders.length?orders.map(o=><Link href={`/account/orders/${o.id}`} key={o.id}><span><strong>{o.orderNumber}</strong><small>{new Date(o.createdAt).toLocaleDateString()} · {o.fulfilmentMethod}</small></span><em>{o.status.replaceAll("_"," ")}</em><b>KES {Number(o.total).toLocaleString()}</b></Link>):<div className="account-empty"><Package/><span><strong>No orders yet</strong><small>Your first order will appear here.</small></span></div>}</section><section className="account-orders account-table" id="prescriptions"><div><h2>My prescriptions</h2><Link href="/prescriptions/upload">Upload another</Link></div><header><span>Prescription</span><span>Status</span><span>Uploaded</span></header>{rx.length?rx.map(p=><a href={`/api/prescriptions/${p.id}/download`} target="_blank" rel="noreferrer" key={p.id}><span><strong>{p.originalFilename}</strong><small>{p.pharmacistNotes||"Open prescription"}</small></span><em>{p.status.replaceAll("_"," ")}</em><b>{new Date(p.createdAt).toLocaleDateString()}</b></a>):<div className="account-empty"><Package/><span><strong>No prescriptions uploaded</strong><small>Review progress will appear here.</small></span></div>}</section><section className="account-products"><header><div><h2>Continue shopping</h2><p>Popular health essentials selected for you.</p></div><Link href="/#products">View all</Link></header><div>{catalog.map(p=><article key={p.id}><Link href={`/products/${p.id}`}><div>{p.imageUrl?<img src={p.imageUrl} alt={p.name}/>:<Package/>}</div><strong>{p.name}</strong><small>{p.packSize||"Healthfield Pharmacy"}</small></Link><footer><b>KES {Number(p.discountPrice??p.price).toLocaleString()}</b><form action="/api/cart" method="post"><input type="hidden" name="productId" value={p.id}/><input type="hidden" name="action" value="add"/><input type="hidden" name="return" value="/account"/><button aria-label={`Add ${p.name} to cart`}><ShoppingCart/></button></form></footer></article>)}</div></section></main>}
